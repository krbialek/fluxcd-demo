name: Build rt-job-server
on:
  workflow_dispatch:

jobs:
  Build_rt-job-server:
    name: Build rt-job-server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup scala
        uses: olafurpg/setup-scala@v11
        with:
          java-version: openjdk@1.18.0

